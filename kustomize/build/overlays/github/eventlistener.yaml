apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: nodejs-dev-demo-listener
spec:
  serviceAccountName: trigger-sa 
  triggers:
    - bindings:
        - ref: nodejs-dev-demo-trigger-binding
      template:
        ref: nodejs-dev-demo-pipeline-template
      interceptors:
      - ref:
          name: github
          kind: ClusterInterceptor
        params:
        - name: "secretRef"
          value:
            secretName: webhooksecret
            secretKey: webhook_secret
        - name: "eventTypes"
          value: ["push"]
