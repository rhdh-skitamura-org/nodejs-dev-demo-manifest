apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: nodejs-dev-demo
  name: nodejs-dev-demo
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: nodejs-dev-demo
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
