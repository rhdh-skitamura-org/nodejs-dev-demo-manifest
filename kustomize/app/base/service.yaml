apiVersion: v1
kind: Service
metadata:
  labels:
    app: nodejs-dev-demo
  name: nodejs-dev-demo
spec:
  ports:
  - name: 8080-tcp
    port: 8080
    protocol: TCP
    targetPort: 3000
  selector:
    app: nodejs-dev-demo
  type: ClusterIP
